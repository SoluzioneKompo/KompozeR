apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot-service
  namespace: kompozer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot-service
  template:
    metadata:
      labels:
        app: chatbot-service
    spec:
      containers:
        - name: chatbot-service
          image: kompozer/chatbot-service:latest
          imagePullPolicy: Never
          env:
            - name: CHATBOT_PORT
              value: "3006"
            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kompozer-secrets
                  key: mongoRootPassword
            - name: CHAT_MONGO_URI
              value: "mongodb://root:$(MONGO_PASSWORD)@mongo-chat:27017/chatbotdb?authSource=admin"
            - name: CATALOG_BASE_URL
              value: "http://catalog-service:3002"
          ports:
            - containerPort: 3006
          readinessProbe:
            tcpSocket:
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: kompozer
spec:
  selector:
    app: chatbot-service
  ports:
    - port: 3006
      targetPort: 3006
