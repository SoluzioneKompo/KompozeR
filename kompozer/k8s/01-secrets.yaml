# Shared credentials for the KompozeR cluster.
# Demo-grade values (Minikube). Rotate before any real deployment.
apiVersion: v1
kind: Secret
metadata:
  name: kompozer-secrets
  namespace: kompozer
type: Opaque
stringData:
  mongoRootUser: root
  mongoRootPassword: changeme
  jwtSecret: "kompozer-dev-secret-key-32-chars!!"
